# 🔧 Install.sh v3.2 - SQLite3 расширение

## Новая ошибка, найденная при тестировании

**Статус**: 🔴 КРИТИЧНО — БД не работает  
**Лог ошибки**:
```
Illuminate\Database\QueryException

could not find driver (Connection: sqlite, SQL: PRAGMA foreign_keys = ON;)
```

**Где падает**: При попытке запустить `php artisan key:generate` или `php artisan serve`

### Причина

PHP не имеет расширение `sqlite3`, которое требуется для подключения к SQLite базе данных. Расширение `sqlite3` отличается от других (xml, dom, curl и т.д.) - его нужно явно проверять и устанавливать.

### Решение в v3.2

Добавляем `sqlite3` в список требуемых расширений:

```bash
REQUIRED_EXTENSIONS=("xml" "dom" "curl" "mbstring" "zip" "sqlite3")
```

Теперь скрипт:
1. Проверяет наличие расширения `sqlite3`
2. Если его нет - устанавливает: `php8.3-sqlite3` (или нужную версию)
3. Это происходит ДО любых artisan команд

---

## Что требуется для Laravel

| Расширение | Назначение | Команда установки |
|-----------|-----------|----------|
| xml | XML парсинг | `sudo apt-get install php8.3-xml` |
| dom | DOM манипуляция | Обычно идет с xml |
| curl | HTTP запросы | `sudo apt-get install php8.3-curl` |
| mbstring | Unicode строки | `sudo apt-get install php8.3-mbstring` |
| zip | ZIP архивы | `sudo apt-get install php8.3-zip` |
| sqlite3 | **SQLite БД** | `sudo apt-get install php8.3-sqlite3` ← НОВОЕ |

---

## История версий

| Версия | Исправления |
|--------|------------|
| v3.0 | PHP версия, PHP расширения, Node версия, exit codes |
| v3.1 | bootstrap/cache, storage директории, права доступа |
| v3.2 | **SQLite3 расширение** ← НОВОЕ |

---

**Статус**: ✅ v3.2 готова к тестированию  
**Дата**: 29 января 2024 г.
