# Быстрая установка Tomodoro

## 🚀 Минимум операций для запуска

### Windows

**Просто двойной клик на `install.bat`**

Всё остальное сделается автоматически:
- ✅ Установка PHP, Node.js, Composer (если не установлены)
- ✅ Установка всех зависимостей
- ✅ Настройка приложения
- ✅ Запуск сервера

После завершения откройте браузер: **http://localhost:8000**

> Требуется права администратора

---

### Linux / macOS

**Одна команда в терминале:**

```bash
chmod +x install.sh && ./install.sh
```

Скрипт сам определит вашу ОС и установит всё необходимое:
- ✅ Определение дистрибутива (Ubuntu, CentOS, macOS)
- ✅ Установка всех зависимостей (PHP, Node.js, Composer)
- ✅ Конфигурация приложения
- ✅ Запуск сервера

После завершения откройте браузер: **http://localhost:8000**

---

## 📋 Что делает установщик?

1. **Проверяет и устанавливает** необходимые инструменты:
   - PHP 8.2+
   - Node.js 20+
   - Composer
   - NPM

2. **Устанавливает зависимости**:
   - `composer install` (PHP библиотеки)
   - `npm install` (JavaScript пакеты)

3. **Конфигурирует приложение**:
   - Создаёт `.env` файл
   - Генерирует ключ приложения
   - Подготавливает базу данных (SQLite)

4. **Собирает фронтенд** и запускает сервер разработки

---

## ✨ Преимущества

- 🎯 **Один клик/одна команда** - не нужно разбираться в зависимостях
- 🔄 **Кроссплатформа** - Windows, Linux, macOS
- 📦 **Полная автоматизация** - установка, конфигурация, запуск
- 🛡️ **Безопасная** - проверяет наличие инструментов перед установкой
- 🚫 **Без хлама** - устанавливает только необходимое

---

## 🆘 Если что-то пошло не так

### Windows

Если скрипт не запускается:
1. Откройте PowerShell от имени администратора
2. Выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
install.bat
```

### Linux / macOS

Если скрипт не запускается:
```bash
bash install.sh
```

Если нужны права sudo:
```bash
sudo bash install.sh
```

---

## 📝 После установки

Приложение будет доступно по адресу: **http://localhost:8000**

Для остановки сервера нажмите: **Ctrl+C**

Чтобы запустить снова в следующий раз:
- **Windows**: двойной клик на `install.bat`
- **Linux/macOS**: `php artisan serve`
