# 🧪 Инструкция по тестированию install.sh v3.0

## Быстрое тестирование на Linux

### На свежей системе (Docker / VM)

```bash
# 1. Клонируем репозиторий
git clone https://github.com/qniman/tomodoro
cd tomodoro

# 2. Запускаем установку
chmod +x install.sh
./install.sh

# 3. Проверяем результат
php -v                    # Должна быть PHP 8.3+
php -m | grep xml         # Должны быть xml расширения
node -v                   # Должна быть Node 20+
npm -v                    # Должен быть NPM 10+
composer --version        # Должен быть Composer 2.6+

# 4. Проверяем что приложение работает
curl http://localhost:8000
```

### Проверка всех критических шагов

После успешной установки скрипт должен был:

- ✅ Установить PHP 8.3 (или выше)
- ✅ Установить расширения: xml, dom, curl, mbstring, zip
- ✅ Установить Node.js 20 LTS (не 18!)
- ✅ Установить npm 10+
- ✅ Установить Composer
- ✅ Скачать PHP зависимости (composer install)
- ✅ Скачать Node зависимости (npm install)
- ✅ Создать .env файл
- ✅ Сгенерировать ключ приложения
- ✅ Создать и заполнить БД (миграции)
- ✅ Собрать фронтенд (npm run build)
- ✅ Запустить сервер на http://localhost:8000

## Проверка на ошибки

### Если composer install упал

Скрипт теперь покажет:
```
[✗] Ошибка установки PHP зависимостей.
Лог ошибки:
  Problem X: ...
Проверьте:
  1. Все ли PHP расширения установлены? (php -m)
  2. Версия PHP совместима? (php -v)
  3. Достаточно памяти? (free -h)
```

**Решение**: Проверьте php -m и убедитесь что есть xml, dom, curl, mbstring, zip

### Если Node.js версия < 20

Скрипт покажет:
```
[!] Node.js версия v18.19.1 < 20. Обновляем для совместимости...
```

И автоматически обновит на Node 20 LTS

### Если npm install упал

Скрипт покажет:
```
[✗] Ошибка установки Node.js зависимостей.
Лог ошибки:
  ERR! ...
```

И выведет последние 20 строк лога

---

## Логи для отладки

После запуска скрипта остаются логи:
- `/tmp/composer.log` — лог composer install
- `/tmp/npm.log` — лог npm install
- `/tmp/npm-build.log` — лог npm run build

Можно просмотреть:
```bash
cat /tmp/composer.log
cat /tmp/npm.log
cat /tmp/npm-build.log
```

---

## Критические проверки v3.0

| Проверка | Команда | Результат |
|---------|---------|-----------|
| PHP версия | `php -v` | PHP 8.3+ |
| PHP расширения | `php -m \| grep -E "xml\|dom\|curl"` | Должны быть все 3 |
| Node версия | `node -v` | v20+ (не v18!) |
| npm версия | `npm -v` | 10+ |
| Composer | `composer --version` | 2.6+ |
| vendor директория | `ls vendor/autoload.php` | Файл существует |
| .env файл | `cat .env` | APP_NAME=Tomodoro |
| Сервер запущен | `curl http://localhost:8000` | Ответ 200 или редирект |

---

## Если что-то не работает

1. **Проверьте что скрипт выполнился до конца без ошибок**
   ```bash
   echo $?  # Должно быть 0 если скрипт завершился успешно
   ```

2. **Проверьте все зависимости вручную**
   ```bash
   php -m
   node -v
   npm -v
   composer --version
   ```

3. **Проверьте логи**
   ```bash
   cat /tmp/composer.log
   cat /tmp/npm.log
   ```

4. **Попробуйте запустить команды вручную**
   ```bash
   composer install --no-interaction
   npm install
   php artisan migrate --force
   npm run build
   ```

5. **Если Debian/Ubuntu: проверьте что Node 20 установлен**
   ```bash
   sudo apt list --installed | grep nodejs
   # Должно быть: nodejs/... 20.x.x
   ```

---

**Версия документа**: 3.0  
**Последнее обновление**: 2024
