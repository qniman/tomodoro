# 🚀 Как загрузить проект на GitHub

## Предварительные требования

1. **Установите Git**: https://git-scm.com/downloads
2. **Создайте аккаунт на GitHub**: https://github.com/signup
3. **Создайте новый репозиторий на GitHub** (без инициализации)

## Пошаговая инструкция

### Шаг 1: Инициализируйте Git в проекте

```bash
cd d:\Проекты\tomodoro
git init
git add .
git commit -m "Initial commit: Tomodoro application with full documentation"
```

### Шаг 2: Добавьте удалённый репозиторий

Замените `USERNAME` на ваше имя пользователя GitHub и `REPO_NAME` на название репозитория:

```bash
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

### Шаг 3: Настройте SSH (опционально, но рекомендуется)

Для безопасного доступа:

```bash
# Генерируйте SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавьте публичный ключ на GitHub:
# https://github.com/settings/keys

# Измените URL на SSH (если хотите)
git remote set-url origin git@github.com:USERNAME/REPO_NAME.git
```

---

## 📝 Что исключено из Git (.gitignore)

Автоматически исключаются:

✅ `/vendor` — PHP зависимости (composer)  
✅ `/node_modules` — JS зависимости (npm)  
✅ `.env` — переменные окружения  
✅ `/storage/logs` — логи приложения  
✅ `/public/build` — собранные ассеты  
✅ `.DS_Store`, `Thumbs.db` — системные файлы  
✅ `*.log` — все логи  

## ✅ Что будет загружено

Включаются в Git:

✅ Весь исходный код приложения  
✅ Вся документация (11+ файлов)  
✅ Конфигурационные файлы  
✅ Миграции базы данных  
✅ Тесты  
✅ `.env.example` (шаблон)  
✅ `composer.json` и `package.json`  

---

## 🔐 Важно: Защита чувствительных данных

### Проверьте перед push:

```bash
# Проверьте, что .env НЕ включится
git status

# Убедитесь, что нет:
# - Пароли
# - API ключи
# - Приватные ключи
# - Токены
```

### Если случайно загрузили чувствительные данные:

```bash
# Немедленно измените пароли и токены на серверах!
# Потом удалите файл из истории Git:
git rm --cached .env
git commit --amend -CHEAD
git push --force-with-lease
```

---

## 📊 Первоначальная загрузка

Первая загрузка обычно содержит:

- **~3,500+ файлов** (исходный код + документация)
- **~2-5 MB** данных (без vendor и node_modules)
- **Время**: 1-5 минут (зависит от скорости интернета)

---

## 🔄 Регулярные операции

### Добавить новые файлы

```bash
git add .
git commit -m "Add new feature: ..."
git push
```

### Обновить существующие файлы

```bash
git add src/
git commit -m "Update: ..."
git push
```

### Создать новую ветку

```bash
git checkout -b feature/new-feature
git push -u origin feature/new-feature

# Потом создайте Pull Request на GitHub
```

---

## 🎯 GitHub Pages (опционально)

Если хотите разместить документацию на GitHub Pages:

1. Перейдите в Settings → Pages
2. Выберите branch: `main`
3. Folder: `/docs`
4. Сохраните

Документация будет доступна по адресу: `https://username.github.io/repo-name`

---

## 🆘 Решение типичных проблем

### Проблема: "fatal: not a git repository"

```bash
# Решение:
git init
git add .
git commit -m "Initial commit"
```

### Проблема: "Permission denied (publickey)"

```bash
# Решение: добавьте SSH ключ на GitHub
# https://github.com/settings/keys

# Или используйте HTTPS вместо SSH
git remote set-url origin https://github.com/USERNAME/REPO_NAME.git
```

### Проблема: ".env включился в Git"

```bash
# Решение:
git rm --cached .env
git commit -m "Remove .env from version control"
git push
```

---

## 📚 Полезные команды Git

```bash
# Просмотр статуса
git status

# Просмотр истории коммитов
git log --oneline

# Просмотр разницы
git diff

# Отмена последнего коммита
git reset --soft HEAD~1

# Просмотр удалённых репозиториев
git remote -v

# Загрузка изменений с сервера
git pull

# Отправка изменений на сервер
git push
```

---

## 🎓 Рекомендации

1. **Коммитьте часто** — небольшие, логичные изменения
2. **Пишите хорошие сообщения коммитов** — опишите, что изменилось
3. **Используйте ветки** — для новых функций
4. **Делайте Pull Requests** — перед merge в main
5. **Синхронизируйте регулярно** — `git pull` перед началом работы

---

## 📞 Нужна помощь?

- [Git документация](https://git-scm.com/doc)
- [GitHub Help](https://help.github.com)
- [GitHub Desktop](https://desktop.github.com) — GUI клиент

---

**Удачи с загрузкой проекта на GitHub!** 🚀
